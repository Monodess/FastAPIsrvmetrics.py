"""
    Pagespeed server metrics Proxy-Worker

Creating minimal api
for accessing what?
1) endpoint post url (to capture metrics from)
2) endpoint get metrics (what contains inputs body?)
    only 2 endp for reading and writing


"""
"""
    Dependency injection

Dependency Injection is a mechanism where an object
receives the dependencies it needs from the outside,
instead of creating them itself.
This removes the need for factories and reduces coupling

"""